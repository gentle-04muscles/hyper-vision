# hyper-vision
Hyper Vision is a simple, read-only tool designed to inspect and validate the Base Sepolia network. It allows developers to quickly verify network connectivity, inspect wallet balances, and validate contract deployments in a safe, non-invasive manner
